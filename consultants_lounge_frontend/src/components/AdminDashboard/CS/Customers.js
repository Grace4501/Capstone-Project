export default function Customers(){
    return(
        <div>
            <button> + Add Customer </button>
            <select>
              <option> Newest</option>
              <option> Oldest </option>
            </select>
        </div>
    )
}